import styled from "styled-components";

const UserBlockStyled = styled.div`
  display: flex;
  align-items: center;

  cursor: pointer;
  align-text: center;
  margin: 0.3em 0;
`;

export default UserBlockStyled;
