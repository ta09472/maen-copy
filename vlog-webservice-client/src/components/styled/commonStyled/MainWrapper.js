import styled from "styled-components";

const MainWrapper = styled.div`
  display: flex;
  flex-direction: row;
  overflow-y: hidden;
  margin-top: 1em;
  min-width: 1380px;
`;

export default MainWrapper;
